import { instanceToPlain } from './instanceToPlain';

describe('instanceToPlain', () => {
  test('exports a function called instanceToPlain', () => {
    expect(instanceToPlain).toBeInstanceOf(Function);
  });
});
