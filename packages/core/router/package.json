{
  "name": "@axuate/router",
  "version": "0.1.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:watch": "tsc -p tsconfig.build.json --watch",
    "lint": "eslint index.ts",
    "test": "jest --passWithNoTests",
    "test:cucumber": "cucumber-js ./test/features"
  },
  "dependencies": {
    "@axuate/http": "*"
  },
  "devDependencies": {
    "@axuate/eslint-config": "*",
    "@axuate/jest": "*",
    "@axuate/typescript": "*",
    "@axuate/cucumber": "*"
  }
}
