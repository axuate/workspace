{
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:cucumber": "turbo run cucumber",
    "lint": "lint-staged",
    "all": "turbo run all"
  },
  "devDependencies": {
    "@axuate/eslint-config": "*",
    "@commitlint/cli": "^17.0.3",
    "@commitlint/config-conventional": "^17.0.3",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1",
    "turbo": "^1.3.1"
  },
  "workspaces": [
    "packages/core/*",
    "packages/tools/*"
  ]
}
